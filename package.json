{
  "name": "test-site",
  "version": "0.0.0",
  "scripts": {
    "build:clean": "del \"dist/!(.git*|Procfile)**\"",
    "prebuild": "npm run build:clean",
    "build": "mv index.html dist"
  },
  "dependencies": {
    "del-cli": "^0.2.1"
  }
}